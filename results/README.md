# Results

`predictions/` contains prediction-only evidence used by the quick verifier. No tweet
text is committed.

`summaries/` contains the compact frozen result tables generated by the corresponding
pipeline stages.

Use the top-level `RESULTS.md` for the paper-level interpretation and file mapping.
